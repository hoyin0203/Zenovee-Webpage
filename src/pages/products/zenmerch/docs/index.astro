---
import BaseLayout from "../../../../layouts/BaseLayout.astro";

const docs = [
  { title: "Getting started", desc: "Install ZenMerch, enable the embed, add the product block, and test.", href: "/products/zenmerch/docs/getting-started" },
  { title: "Theme integration (OS 2.0)", desc: "Enable the embed and add the theme app block to product templates.", href: "/products/zenmerch/docs/theme-integration" },
  { title: "Templates + targeting", desc: "Create a template once and apply it across products using rules.", href: "/products/zenmerch/docs/templates-targeting" },
  { title: "Webhooks + CSV export", desc: "Production-ready operations: webhooks and CSV-friendly exports.", href: "/products/zenmerch/docs/webhooks-exports" },
  { title: "Troubleshooting", desc: "Block not showing? Changes not updating? Fix common setup issues fast.", href: "/products/zenmerch/docs/troubleshooting" },
];
---

<BaseLayout title="ZenMerch Docs" description="Public documentation for ZenMerch—built to be Shopify-review friendly.">
  <section class="container-pad py-14 sm:py-20">
    <div class="reveal" data-reveal>
      <h1 class="text-4xl font-semibold tracking-tight sm:text-5xl">ZenMerch documentation</h1>
      <p class="mt-4 max-w-2xl text-white/70">
        Public docs for merchants and Shopify reviewers: setup guide, best-practice placement, troubleshooting, and support contact.
      </p>
      <div class="mt-7 flex flex-col gap-3 sm:flex-row">
        <a class="btn btn-primary" href="/products/zenmerch/docs/getting-started">Getting started</a>
        <a class="btn btn-secondary" href="/products/zenmerch/faq">FAQ</a>
        <a class="btn btn-secondary" href="/contact">Contact support</a>
      </div>
    </div>

    <div class="card reveal mt-10 p-6 sm:p-8" data-reveal>
      <h2 class="text-xl font-semibold tracking-tight">Setup checklist (recommended)</h2>
      <ol class="mt-4 grid gap-2 text-sm text-white/70">
        <li><span class="text-white/85 font-semibold">1)</span> Install ZenMerch and open it in Shopify admin.</li>
        <li><span class="text-white/85 font-semibold">2)</span> Enable the ZenMerch app embed in Theme Editor (App embeds → enable → Save).</li>
        <li><span class="text-white/85 font-semibold">3)</span> Add the ZenMerch app block to your product template (above buy buttons).</li>
        <li><span class="text-white/85 font-semibold">4)</span> Create a template: fields + layers, link them, Save.</li>
        <li><span class="text-white/85 font-semibold">5)</span> Apply to products using Products assignment or Rules (tags/title/vendor/type).</li>
        <li><span class="text-white/85 font-semibold">6)</span> Test on storefront (preview + validation + uploads), place a test order.</li>
      </ol>
      <div class="mt-6 flex flex-col gap-3 sm:flex-row">
        <a class="btn btn-primary" href="/products/zenmerch/docs/theme-integration">Theme integration steps</a>
        <a class="btn btn-secondary" href="/products/zenmerch/features">ZenMerch features</a>
      </div>
    </div>

    <div class="mt-10 grid gap-4 md:grid-cols-2">
      {docs.map((d) => (
        <a class="card reveal block p-6 hover:bg-white/[0.05]" data-reveal href={d.href}>
          <div class="text-base font-semibold">{d.title}</div>
          <p class="mt-2 text-sm text-white/65">{d.desc}</p>
        </a>
      ))}
    </div>
  </section>
</BaseLayout>

