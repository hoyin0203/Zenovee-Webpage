---
import BaseLayout from "../layouts/BaseLayout.astro";
---

<BaseLayout
  title="Zenovee"
  description="A multi-product Shopify-first studio. Explore ZenMerch—our product personalization app—plus services for websites, webapps, and Shopify builds."
>
  <section class="container-pad py-14 sm:py-20">
    <div class="grid gap-10 lg:grid-cols-12 lg:items-center">
      <div class="lg:col-span-7">
        <div class="reveal" data-reveal>
          <p class="inline-flex items-center gap-2 rounded-full border border-white/10 bg-white/[0.03] px-3 py-1 text-xs text-white/70">
            ZenMerch by Zenovee · “Personalization made powerful, simple, and instant.”
          </p>
          <h1 class="hero-title mt-5 text-balance text-4xl font-semibold tracking-tight sm:text-5xl">
            <span class="hero-word" style="--i:0">Launch</span>
            <span class="hero-word" style="--i:1">personalization</span>
            <span class="hero-word" style="--i:2">on</span>
            <span class="hero-word" style="--i:3">Shopify</span>
            <span class="hero-word" style="--i:4">—</span>
            <span class="hero-word" style="--i:5">without</span>
            <span class="hero-word" style="--i:6">messy</span>
            <span class="hero-word" style="--i:7">theme</span>
            <span class="hero-word" style="--i:8">edits.</span>
          </h1>
          <p class="mt-4 max-w-2xl text-pretty text-base text-white/70 sm:text-lg">
            ZenMerch is a Shopify product personalization app for Online Store 2.0: build personalizers with fields + layers, show live preview,
            enforce validation + profanity filtering, reuse templates with targeting rules, and power fulfillment with webhooks + CSV export.
          </p>
          <div class="mt-6 flex flex-wrap gap-2 text-xs text-white/70">
            <span class="rounded-full border border-white/10 bg-white/[0.03] px-3 py-1">Print-on-demand</span>
            <span class="rounded-full border border-white/10 bg-white/[0.03] px-3 py-1">Made-to-order</span>
            <span class="rounded-full border border-white/10 bg-white/[0.03] px-3 py-1">Online Store 2.0</span>
            <span class="rounded-full border border-white/10 bg-white/[0.03] px-3 py-1">Templates + targeting</span>
            <span class="rounded-full border border-white/10 bg-white/[0.03] px-3 py-1">Webhooks + exports</span>
          </div>
          <div class="mt-7 flex flex-col gap-3 sm:flex-row">
            <a class="btn btn-primary" href="/products/zenmerch">Explore ZenMerch</a>
            <a class="btn btn-secondary" href="/products/zenmerch/docs/getting-started">Read setup guide</a>
            <a class="btn btn-secondary" href="/contact">Talk to us</a>
          </div>
        </div>
      </div>

      <div class="lg:col-span-5">
        <div class="card reveal overflow-hidden p-5" data-reveal>
          <div class="flex items-center justify-between">
            <div class="text-sm font-semibold">ZenMerch</div>
            <span class="rounded-full bg-brand-500/15 px-2 py-1 text-xs font-semibold text-brand-200 ring-1 ring-brand-500/25">App</span>
          </div>
          <p class="mt-3 text-sm text-white/65">
            Build a personalizer with fields + layers, validate inputs, reuse templates with targeting rules, and export production-ready data.
          </p>
          <div id="homeHeroMedia" class="hero-media relative mt-5 w-full">
            <img
              class="w-full rounded-xl border border-white/10 bg-white/[0.02]"
              src="/images/zenmerch/zenmerch-hero.jpg"
              alt="ZenMerch storefront personalized product"
              loading="lazy"
              decoding="async"
            />
          </div>
          <div class="mt-5 flex gap-2">
            <a class="btn btn-secondary w-full" href="/products/zenmerch/features">Features</a>
            <a class="btn btn-primary w-full" href="/products/zenmerch/docs">Docs</a>
          </div>
        </div>
      </div>
    </div>
  </section>

  <section class="container-pad pb-14 sm:pb-20">
    <div class="reveal" data-reveal>
      <div class="card overflow-hidden border border-white/10 bg-white/[0.02] p-0 md:grid md:grid-cols-2">
        <div class="p-8 md:p-10 lg:p-12">
          <div class="flex items-center justify-between">
            <h2 class="text-2xl font-semibold tracking-tight">ZenCraft Gifts</h2>
            <span class="rounded-full bg-white/5 px-2 py-1 text-xs font-semibold text-white/70 ring-1 ring-white/10">Shop</span>
          </div>
          <p class="mt-4 text-pretty text-white/70">
            A real-world Shopify storefront selling personalized crafting products and curated gift sets.
            See our apps in action and buy beautiful things.
          </p>
          <div class="mt-8">
            <a class="btn btn-primary w-full md:w-auto" href="https://zencraftgifts.com" target="_blank" rel="noreferrer">
              Visit shop →
            </a>
          </div>
        </div>
        <div class="relative min-h-[240px] border-t border-white/10 bg-zinc-950/50 md:border-l md:border-t-0">
          <img
            class="absolute inset-0 h-full w-full object-cover opacity-80 transition hover:opacity-100"
            src="/images/zencraft/zencraft-hero.png"
            alt="ZenCraft Gifts shop preview"
            loading="lazy"
          />
        </div>
      </div>
    </div>
  </section>

  <section class="container-pad pb-14 sm:pb-20">
    <div class="reveal" data-reveal>
      <h2 class="text-2xl font-semibold tracking-tight">Services</h2>
      <p class="mt-2 max-w-2xl text-sm text-white/65">
        Need a website, a web app, or Shopify development? We can help—from discovery to launch and ongoing support.
      </p>

      <div class="mt-8 grid gap-4 md:grid-cols-3">
        <a class="card block p-6 hover:bg-white/[0.05]" href="/services/website-development">
          <div class="text-base font-semibold">Website development</div>
          <p class="mt-2 text-sm text-white/65">Fast, SEO-friendly marketing sites with great UX.</p>
          <img class="mt-4 w-full rounded-xl border border-white/10 bg-white/[0.02]" src="/images/services/website.png" alt="" loading="lazy" />
        </a>
        <a class="card block p-6 hover:bg-white/[0.05]" href="/services/webapp-development">
          <div class="text-base font-semibold">Web app development</div>
          <p class="mt-2 text-sm text-white/65">Dashboards, portals, and internal tools built for scale.</p>
          <img class="mt-4 w-full rounded-xl border border-white/10 bg-white/[0.02]" src="/images/services/webapp.png" alt="" loading="lazy" />
        </a>
        <a class="card block p-6 hover:bg-white/[0.05]" href="/services/shopify-development">
          <div class="text-base font-semibold">Shopify development</div>
          <p class="mt-2 text-sm text-white/65">Themes, extensions, integrations, and automation.</p>
          <img class="mt-4 w-full rounded-xl border border-white/10 bg-white/[0.02]" src="/images/services/shopify.png" alt="" loading="lazy" />
        </a>
      </div>
    </div>
  </section>

  <section class="container-pad pb-20">
    <div class="card reveal p-8 md:p-10" data-reveal>
      <div class="grid gap-8 md:grid-cols-2 md:items-center">
        <div>
          <h2 class="text-2xl font-semibold tracking-tight">Shopify submission ready</h2>
          <p class="mt-3 text-sm text-white/65">
            We keep key pages public and easy to find: ZenMerch pricing, documentation, FAQ, privacy policy, terms, and support contact.
          </p>
          <div class="mt-6 flex flex-col gap-3 sm:flex-row">
            <a class="btn btn-primary" href="/products/zenmerch/docs">Read ZenMerch docs</a>
            <a class="btn btn-secondary" href="/legal/privacy-policy">Privacy policy</a>
          </div>
        </div>
        <div class="grid gap-3">
          <div class="rounded-xl border border-white/10 bg-white/[0.02] p-4 text-sm text-white/75">
            <div class="font-semibold text-white">Need help?</div>
            <div class="mt-1">Use the contact form or email support—response-focused and reviewer-friendly.</div>
          </div>
          <div class="rounded-xl border border-white/10 bg-white/[0.02] p-4 text-sm text-white/75">
            <div class="font-semibold text-white">One domain strategy</div>
            <div class="mt-1">Marketing stays on paths. App backend lives on a subdomain.</div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <style>
    .hero-title {
      line-height: 1.05;
    }
    .hero-word {
      display: inline-block;
      margin-right: 0.18em;
      opacity: 0;
      transform: translateY(18px) rotateX(22deg);
      filter: blur(6px);
      animation: hero-word-in 900ms cubic-bezier(0.2, 0.7, 0.2, 1) forwards;
      animation-delay: calc(var(--i) * 60ms + 80ms);
      transform-origin: 50% 80%;
    }
    /* Only animate once content is in-view (ties into ScrollReveal) */
    .reveal:not(.in-view) .hero-word {
      animation: none;
    }
    @keyframes hero-word-in {
      0% {
        opacity: 0;
        transform: translateY(18px) rotateX(22deg);
        filter: blur(6px);
      }
      100% {
        opacity: 1;
        transform: translateY(0) rotateX(0deg);
        filter: blur(0);
      }
    }

    .hero-media {
      transform: perspective(900px) rotateX(var(--heroRx, 0deg)) rotateY(var(--heroRy, 0deg))
        translateY(var(--heroTy, 0px));
      transition: transform 220ms cubic-bezier(0.2, 0.7, 0.2, 1);
      will-change: transform;
    }
    .hero-media::before {
      content: "";
      position: absolute;
      inset: -1px;
      border-radius: 1rem;
      pointer-events: none;
      opacity: 0;
      transition: opacity 200ms ease;
      background: radial-gradient(420px circle at var(--px, 50%) var(--py, 50%), rgba(34, 211, 238, 0.14), rgba(0, 0, 0, 0) 55%);
    }
    .hero-media:hover::before {
      opacity: 1;
    }
    .reveal.in-view .hero-media {
      animation: hero-media-in 520ms cubic-bezier(0.2, 0.7, 0.2, 1) both;
    }
    @keyframes hero-media-in {
      0% {
        transform: perspective(900px) translateY(10px) scale(0.99);
        opacity: 0.85;
      }
      100% {
        transform: perspective(900px) translateY(0) scale(1);
        opacity: 1;
      }
    }

  </style>

  <script is:inline>
    (() => {
      const init = () => {
        const el = document.getElementById("homeHeroMedia");
        if (!el) return;
        if (el.dataset.bound === "1") return;
        el.dataset.bound = "1";

        let raf = 0;
        const onMove = (ev) => {
          const rect = el.getBoundingClientRect();
          const x = (ev.clientX - rect.left) / rect.width;
          const y = (ev.clientY - rect.top) / rect.height;
          const ry = (x - 0.5) * 10;
          const rx = (0.5 - y) * 8;

          cancelAnimationFrame(raf);
          raf = requestAnimationFrame(() => {
            el.style.setProperty("--heroRx", rx.toFixed(2) + "deg");
            el.style.setProperty("--heroRy", ry.toFixed(2) + "deg");
            el.style.setProperty("--heroTy", "-2px");
            el.style.setProperty("--px", (x * 100).toFixed(2) + "%");
            el.style.setProperty("--py", (y * 100).toFixed(2) + "%");
          });
        };

        const onLeave = () => {
          el.style.setProperty("--heroRx", "0deg");
          el.style.setProperty("--heroRy", "0deg");
          el.style.setProperty("--heroTy", "0px");
        };

        el.addEventListener("pointermove", onMove);
        el.addEventListener("pointerleave", onLeave);
      };

      if (!window.__zenoveeHomeHeroBound) {
        window.__zenoveeHomeHeroBound = true;
        document.addEventListener("astro:page-load", init);
        document.addEventListener("astro:after-swap", init);
      }
      init();
    })();
  </script>
</BaseLayout>

